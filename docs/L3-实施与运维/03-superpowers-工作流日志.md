# L3-03 Superpowers 工作流日志

更新时间：2026-03-27

## 日志格式

1. 时间
2. 阶段（brainstorming / writing-plans / executing-plans / requesting-code-review / verification-before-completion）
3. 本次输入
4. 本次输出
5. 风险与决策

## 2026-03-27

### 阶段：brainstorming

输入：三角色全小程序重构诉求。

输出：目标与边界冻结，确认优先级为 1-2 周可用版。

风险与决策：先做权限基础设施，避免先改页面造成返工。

### 阶段：writing-plans

输入：现有页面、云函数与文档结构。

输出：形成 4 个批次执行计划（见 L3-02）。

风险与决策：每批必须同步三级文档。

### 阶段：executing-plans（进行中）

输入：当前项目代码基线。

输出：已完成治理层重构（.github 规范、三级文档骨架、回归清单修正）。

风险与决策：后续代码批次必须附带权限回归记录。

### 阶段：executing-plans（批次A完成）

输入：角色化改造计划（登录分流、页面守卫、角色入口）。

输出：已落地 RBAC 基础工具、登录按角色分流、管理与仓库页面守卫、个人页角色入口。

风险与决策：财务入口已开放导航，下一批需尽快补后端财务接口权限闸门。

### 阶段：executing-plans（批次B进行中）

输入：云函数高风险接口清单。

输出：已在 cloudfunctions/api 增加统一角色校验助手与关键写操作审计日志；已收紧派单和删除接口权限。

风险与决策：GitHub 远程推送当前被拒绝（仓库写权限不足），先本地持续迭代，权限开通后统一 push。

### 阶段：executing-plans（批次C进行中）

输入：财务最小闭环目标（报销审核列表 + 审核动作）。

输出：云函数新增财务审核接口，小程序报销页新增财务审核模式与通过/拒绝动作。

风险与决策：当前审核动作为无备注快捷审核，下一迭代增加审核意见输入与拒绝原因模板。

### 阶段：executing-plans（批次D进行中）

输入：继续项目开发，优先补齐奖金/绩效数据权限边界。

输出：云函数已实现按角色的数据可见性控制（管理与财务看全量，执行者仅看本人）。

风险与决策：历史数据若缺少 openId/employeeId 归属字段，执行者可能暂时看不到旧记录；后续需补一次数据归属字段迁移脚本。

### 阶段：verification-before-completion（进行中）

输入：用户要求推进到可测试收口，并提供详细测试手册。

输出：新增全功能测试手册（L3-04），覆盖三角色、权限校验、审计验证和发布前闭环检查。

风险与决策：本地可执行自动化校验优先做“静态+路由一致性检查”；微信端真机交互由手册驱动人工验收。
